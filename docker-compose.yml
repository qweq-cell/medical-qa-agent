version: '3.8'

services:
  medguardian:
    build: .
    ports:
      - "8000:8000"   # FastAPI
      - "8501:8501"   # Streamlit
    environment:
      - NEO4J_URI=bolt://44.203.170.211:7687
      - NEO4J_USER=neo4j
      - NEO4J_PASSWORD=interfaces-projectiles-flash
    volumes:
      - ./data:/app/data